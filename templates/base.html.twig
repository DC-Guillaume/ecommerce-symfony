<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{% block title %}DiY District - Tissus Japonais{% endblock %}</title>
    <link
      rel="stylesheet"
      href="https://use.fontawesome.com/releases/v5.13.0/css/all.css"
    />
    <link href="{{ asset('assets/css/style.css') }}" rel="stylesheet" />
  </head>

  <body>
    <div class="main-wrapper">
      <header>

      <!-- Début Top bar -->
      {% if block('topbar') is defined %}
      <div class="topbar">
        <div class="topbar-contact">
          <a href="/contact">Contact</a>
        </div>
        {% if app.user %}
        <div class="topbar-login">
          <a href="{{path('account')}}"class="link">Mon compte <small>({{app.user.lastname}})</small></a>
          <a href="{{path('app_logout')}}"class="link">Déconnexion</a>
        </div>
        {% else %}
        <div class="topbar-register">
          <a href="{{path('app_login')}}"class="link">Connexion</a>
          <a href="{{path('register')}}"class="link">Inscription</a>
        </div>
        {% endif %}
      </div >
      {% endif %}
      <!-- Fin Top bar -->

      <!-- Début Navbar -->
      {% if block('navbar') is defined %}
      <div class="navbar">
        <a href="/">diy district</a>
        <div class="onglets">
          <p class="link">nouveautés</p>
          <p class="link">tissus</p>
          <p class="link">à propos</p>
          <p class="link">blog</p>
          <p><i class="far fa-heart"></i></p>
          <p><i class="fas fa-shopping-cart"></i></p>
        </div>
      </div>
      {% endif %}
      <!-- Fin NavBar -->

      <!-- Début slider -->
      {% if block('slider') is defined %}
      <div class="slider">
        <h1>Vente de tissus Japonais en ligne</h1>
        <button>nos produits <i class="fas fa-paper-plane"></i></button>
      </div>
      {% endif %}
      </header>
      <!-- Fin slider -->

      <!-- Fin header -->

      <!-- Début Section principale -->

      <main>
      {% block content %}
      {% endblock %}
      </main>

      <!-- Fin section principale -->

      <!-- Début Footer -->

      
      {% if block('footer') is defined %}
      <footer>
        <p>copyright 2022 &copy; diydistrict - site réalise par diydistrict.com</p>
        <div class='informations'>
          <a href="/">CGV-CGU</a><a href="/">Mentions légales</a><a href="/">Politique de confidentialité</a>
        </div>
      </footer>
      {% endif %}
      

      <!-- Fin Footer-->
    </div>
  </body>
</html>
